import 'package:flutter/material.dart';

class Branch1Widget extends StatelessWidget {
  const Branch1Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      width: 200,
      height: 100,
    );
  }
}